export enum ErrorResponse {
  SignIn = 401,
  NotConnected = 403,
  NotFound = 404,
}

export enum APIRespose {
  SUCCESS = 200,
  BAD_REQUEST = 400,
  UNAUTHORIZED = 401,
  NOT_FOUND = 404,
  SERVER_ERROR = 500,
}
